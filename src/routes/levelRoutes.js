////////// SECTION B //////////

// levelController.js, levelModel.js, levelRoutes.js is for managing levels and maps in the PvE aspect

const express = require("express");
const router = express.Router();
const controller = require("../controllers/levelController");

//////////////////////////////////////////////////////////////
// 1 - GET all maps
//////////////////////////////////////////////////////////////
router.get("/", controller.readAllMaps);

//////////////////////////////////////////////////////////////
// 2 - GET all levels in a map by map_id
//////////////////////////////////////////////////////////////
router.get("/:map_id", controller.readAllLevelsInMap);

///// exporting /////
module.exports = router;

//////////////////////////////////////////////////////////////
//////////////////////////// DONE! ///////////////////////////
//////////////////////////////////////////////////////////////
