////////// SECTION B //////////

// levelController.js, levelModel.js, levelRoutes.js is for managing levels and maps in the PvE aspect

const pool = require("../services/db");

//////////////////////////////////////////////////////////////
// getAllMaps
//////////////////////////////////////////////////////////////
module.exports.getAllMaps = (callback) => {
  const SQLSTATMENT = `
    SELECT * FROM Map;
    `;

  pool.query(SQLSTATMENT, callback);
};

//////////////////////////////////////////////////////////////
// getAllLevelsInMap
//////////////////////////////////////////////////////////////
module.exports.getAllLevelsInMap = (data, callback) => {
  const SQLSTATMENT = `
    SELECT * FROM Levels
    WHERE map_id = ?; 
    `;

  const VALUES = [data.map_id];

  pool.query(SQLSTATMENT, VALUES, callback);
};

//////////////////////////////////////////////////////////////
//////////////////////////// DONE! ///////////////////////////
//////////////////////////////////////////////////////////////
